export const environment = {
  production: false,
  // For easier development with a backend server, I've commented the back-end URL and it's being used a proxy configuration.
  // Just run `npm run start:proxy` and make sure that the `proxy.conf.json`
  // More details at: https://angular.io/guide/build#proxying-to-a-backend-server
  // apiUrl: 'http://localhost:8080/api/',
  apiUrl: 'api/',
  appName: "Andre's Portfolio Angular",
};
